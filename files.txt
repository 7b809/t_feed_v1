FILES TO ADD
============

1. api/chart_routes.py
   Purpose:
   - New FastAPI route file for chart dashboard.
   - Serves /chart page.
   - Provides available instruments list for dropdown.
   - Provides candle data for selected instrument.

   New routes:
   - GET /chart
   - GET /chart/instruments
   - GET /chart/candles


2. templates/chart_dashboard.html
   Purpose:
   - New frontend chart UI.
   - Shows instrument dropdown.
   - Shows interval dropdown.
   - Shows candlestick chart.
   - Shows Opening Range levels on chart:
     - OR High
     - OR Low
     - OR Average
     - R1, R2, R3
     - S1, S2, S3
   - Shows right-side panel with selected instrument details and OR values.


FILES TO MODIFY
===============

3. main.py
   Purpose:
   - Import the new chart router.
   - Register chart router with FastAPI app.

   Required changes:
   - Add import:
     from api.chart_routes import router as chart_router

   - Add router registration:
     app.include_router(chart_router)


OPTIONAL FILES TO MODIFY
========================

4. README.md
   Purpose:
   - Document the new chart dashboard.
   - Add /chart URL.
   - Add /chart/instruments and /chart/candles API explanation.
   - Mention that /chart uses Opening Range context from existing OR APIs.

   Suggested section:
   - Chart Dashboard
   - URL: http://127.0.0.1:8000/chart
   - Features:
     - Select instrument
     - View intraday candles
     - Overlay OR High/Low/Avg
     - Overlay R1/R2/R3 and S1/S2/S3


5. api/ws_docs_routes.py
   Purpose:
   - Optional only.
   - Add chart dashboard route info to documentation endpoint.
   - Not required for functionality.


6. templates/index.html
   Purpose:
   - Optional only.
   - Add button/link from existing dashboard to new chart dashboard.

   Example button text:
   - Open Candle Chart Dashboard
   - Link target: /chart


EXISTING FILES USED BUT NOT MODIFIED
====================================

7. services/option_service.py
   Used for:
   - Reading options_cache.
   - Getting available instruments for dropdown.

   No change required if options_cache already has:
   - data
   - subscribed_keys
   - nearest_expiry


8. services/history_service.py
   Used for:
   - Reusing candle response helper logic.
   - Reusing intraday interval conversion logic.

   No change required if chart_routes.py imports existing helpers.


9. services/opening_range_service.py
   Used indirectly through existing route:
   - /opening-range/ema-context

   No change required because chart page can call existing Opening Range API.


10. api/opening_range_routes.py
    Used by frontend:
    - GET /opening-range/ema-context?instrument_key=...

    No change required because this route already returns OR range and levels.


MINIMUM REQUIRED CHANGE SET
===========================

Add:
- api/chart_routes.py
- templates/chart_dashboard.html

Modify:
- main.py

Optional:
- README.md
- templates/index.html
- api/ws_docs_routes.py